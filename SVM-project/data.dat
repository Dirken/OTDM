data;

param numPoints := 250;    
param numVars := 4; 
param dimensions := 5;
param nu := 1000;


read {m in 1..numPoints, n in 1..dimensions} train[m,n] < "data/train.dat";

for{i in 1..numPoints} {
	for{j in 1..numVars} {
		let x[i,j] := train[i,j];	
	}
	let y[i] := train[i,numVars+1];
} 
